Sources for the plugin `plugin.appodeal.Bidmachine`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.appodeal.Bidmachine" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
